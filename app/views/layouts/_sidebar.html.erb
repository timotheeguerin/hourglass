<% if user_signed_in? %>
    <%= react_component('RepositoriesBox', {user_id: current_user.id}, {class: 'all-repositories'}) %>

    <%= react_component('FilesBox', {user_id: current_user.id, repository_id: 3}, {class: 'all-files'}) %>

<% end %>